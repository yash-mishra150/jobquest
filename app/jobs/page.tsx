import * as React from 'react';

interface pageProps {
}

const page = ({ }: pageProps) => {
  return (
   <div>
     <h1>page</h1>
     <p>Welcome to the page component!</p>
   </div>
  );
};

export default page;