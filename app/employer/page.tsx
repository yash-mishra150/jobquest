import Sidebar from '@/components/Sidebar/Sidebar';
import * as React from 'react';

interface pageProps {
}

const page = ({ }: pageProps) => {
  return (
   <div className="">
     <Sidebar/>
   </div>
  );
};

export default page;